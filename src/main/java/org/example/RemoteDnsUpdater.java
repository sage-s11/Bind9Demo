package org.example;

public class RemoteDnsUpdater {
    public static void sendDnsRecord(DnsRecord record) {
        // Simulating sending data to the DNS server (Replace this with actual BIND9 integration)
        System.out.println("📡 Sending to DNS: " + record);
    }
}
